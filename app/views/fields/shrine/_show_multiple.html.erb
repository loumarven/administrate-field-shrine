<% attachments.each do |attachment| %>
  <% if attachment.image? -%>
    <% url = derivative.nil? ? 
      attachment.attachment_url : attachment.attachment_url(derivative) %>
    <%= image_tag url if attachment.attachment_url.present? %>
  <% else -%>
    <%#= TODO: show application icon for PDF/PSD %>
  <% end -%>
<% end %>
